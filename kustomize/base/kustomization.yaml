apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml          # Keep for backward compatibility
  - virtualservice.yaml   # Istio VirtualService (replaces ingress)
  - configmap.yaml

namespace: default
